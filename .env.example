# 复制为 .env 后填写。所有变量只被网关进程（gateway/server.mjs）读取，
# 绝不进入前端代码或提交到仓库。.env 已在 .gitignore 中。

# 大脑：Anthropic Messages API（M1 必需，缺失时前端降级为离线规划模式）
ANTHROPIC_API_KEY=

# 高德开放平台 Key（M1 可选，缺失时 POI 走内置目的地知识库并标注"知识库数据"）
AMAP_API_KEY=

# 网关监听端口（默认 8787）
GATEWAY_PORT=8787
